
serviceAccount: 'projects/$PROJECT_ID/serviceAccounts/cicd-test1-492416-gcb@cicd-test1-492416.iam.gserviceaccount.com'
steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', '$_REGION-docker.pkg.dev/$PROJECT_ID/cicd-demo/spring-petclinic:$_COMMIT_SHA', '.']

  # Push the container image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', '$_REGION-docker.pkg.dev/$PROJECT_ID/cicd-demo/spring-petclinic:$_COMMIT_SHA']

  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'spring-petclinic'
      - '--image'
      - '$_REGION-docker.pkg.dev/$PROJECT_ID/cicd-demo/spring-petclinic:$_COMMIT_SHA'
      - '--region'
      - '$_REGION'
      - '--allow-unauthenticated'

images:
  - '$_REGION-docker.pkg.dev/$PROJECT_ID/cicd-demo/spring-petclinic:$_COMMIT_SHA'

options:
  requestedVerifyOption: VERIFIED
  logging: CLOUD_LOGGING_ONLY
